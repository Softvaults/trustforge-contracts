[package]
name = "trustforge_errors"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"
description = "Shared error types for all TrustForge smart contracts"

[lib]
crate-type = ["lib"]

[dependencies]
soroban-sdk = { version = "22.0" }

[dev-dependencies]
soroban-sdk = { version = "22.0", features = ["testutils"] }
