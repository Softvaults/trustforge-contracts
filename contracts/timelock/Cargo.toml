[package]
name = "timelock"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"
description = "TrustForge timelock — enforces delay before protocol parameter changes take effect"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "22.0"
trustforge_errors = { path = "../trustforge_errors" }

[dev-dependencies]
soroban-sdk = { version = "22.0", features = ["testutils"] }
