[package]
name = "trustforge_math"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"
description = "Shared overflow-safe arithmetic helpers for TrustForge contracts."

[lib]
crate-type = ["rlib"]

[dependencies]
ethnum = { version = "1.5.2", default-features = false }
trustforge_errors = { path = "../trustforge_errors" }
